# Setup

This skill draws on two layers:

1. **Brand** — the project's top-level `DESIGN.md` (palette, type, logo, footer — the brand source of truth)
2. **Skill config** — `~/.config/recoup-internal-consulting-graphics/.env` (template, dimensions, platform)

Just run the skill — it walks you through setup on first use.
