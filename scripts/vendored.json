{
  "_comment": "Maps each canonical shared file to its byte-identical vendored copies, kept in sync by scripts/check_vendored.py. See AGENTS.md 'Portable Skill Contract'. After the collapse into a single flat plugin at the repo root there are no cross-plugin vendored copies; each skill vendors its own references inside its own directory. This list is intentionally empty.",
  "groups": []
}
