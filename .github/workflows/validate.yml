name: validate

on:
  pull_request:
  push:
    branches: [main]

jobs:
  portability:
    name: Portability + drift + manifests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Portable Skill Contract lint
        run: python3 scripts/portability_lint.py
      - name: Vendored-file drift check
        run: python3 scripts/check_vendored.py
      - name: Manifest + parity validation
        run: python3 scripts/validate_manifests.py
      # Single flat plugin at the repo root (skills/ + RESOLVER.md); the focused
      # source plugins were consolidated into it, so there is no bundle-drift gate.
      - name: Resolver reachability (no dark skills)
        run: python3 scripts/check_resolvable.py
      - name: Resolver eval (routing fixtures + coverage)
        run: python3 scripts/run_resolver_eval.py
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - name: Consulting media and runtime regression checks
        run: node --test tests/consulting-brand.test.mjs tests/consulting-runtime.test.mjs
      - name: Consulting template and credential boundary checks
        run: python3 -m unittest discover -s tests -p test_consulting_templates.py
