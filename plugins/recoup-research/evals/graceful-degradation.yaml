name: graceful-degradation
skill: recoup-web-research
input: "Research an extremely obscure local artist the provider won't have"
expected:
  - fallback to web research endpoints
  - structured findings despite no structured provider data
  - clear indication that primary data was unavailable
must_call:
  - '"GET /research?q=" (initial attempt)'
  - '"POST /research/web" (first fallback)'
must_not:
  - '"I couldn''t find any data" without trying web fallback'
  - fabricating structured fields when search returns empty results
  - giving up after one empty response
